# Модуль управления ограничениями VESC

## ✅ Что сделано

Добавлен модуль для работы с ограничениями мотора VESC (максимальный ток, скорость, мощность).

### Новые файлы:
- `src/vesc_limits.h` - заголовочный файл с API
- `src/vesc_limits.cpp` - реализация модуля
- `VESC_LIMITS_USAGE.md` - подробная документация (английский)

### Изменения в существующих файлах:
- `src/debug_log.h` - добавлен `LOG_LIMITS_ENABLED` для логирования
- `src/datatypes.h` - добавлены COMM_DETECT_HALL_FOC и другие команды

## 🎯 Основные возможности

### 1. Через VESC Tool (BLE)
- ✅ **Работает уже сейчас** - все команды пересылаются через BLE bridge
- Можете читать и изменять ВСЕ параметры конфигурации мотора
- Команды `COMM_GET_MCCONF` и `COMM_SET_MCCONF` полностью поддерживаются

### 2. Программный API (C++)

```cpp
// Инициализация
vesc_limits_init();

// Запросить лимиты с VESC
vesc_limits_request(0);  // vesc_id = 0

// Проверить и прочитать
if (vesc_limits_is_valid()) {
    const vesc_motor_limits_t* limits = vesc_limits_get();
    Serial.printf("Макс. ток: %.2f A\n", limits->l_current_max);
}

// Установить максимальный ток
vesc_limits_set_current_max(0, 80.0, 60.0);  // мотор 80A, батарея 60A

// Установить максимальную скорость
vesc_limits_set_speed_max(0, 60000.0);  // 60000 ERPM

// Установить максимальную мощность
vesc_limits_set_power_max(0, 3000.0);  // 3000W
```

## 📋 Параметры ограничений

| Параметр | Описание | Тип |
|----------|----------|-----|
| `l_current_max` | Максимальный ток мотора | float (A) |
| `l_current_min` | Максимальный ток торможения | float (A, отрицательный) |
| `l_in_current_max` | Максимальный ток от батареи | float (A) |
| `l_in_current_min` | Максимальный ток рекуперации | float (A, отрицательный) |
| `l_watt_max` | Максимальная мощность | float (W) |
| `l_watt_min` | Максимальная мощность торможения | float (W, отрицательный) |
| `l_erpm_max` | Максимальная скорость (ERPM) | float |
| `l_erpm_min` | Минимальная скорость (ERPM) | float (отрицательный для реверса) |

## 🚀 Как использовать через VESC Tool

### Шаг 1: Подключение
1. Запустите VESC Tool
2. Выберите Bluetooth → Scan
3. Найдите "SuperVESCDisplay" и подключитесь

### Шаг 2: Чтение конфигурации
1. Перейдите на вкладку "Motor Configuration"
2. Нажмите "Read Motor Configuration"
3. Все параметры будут загружены с VESC контроллера

### Шаг 3: Изменение лимитов
1. **Current Limits** (раздел с токами):
   - Motor Current Max: макс. ток мотора
   - Battery Current Max: макс. ток от батареи
   
2. **ERPM Limits** (раздел со скоростью):
   - ERPM Max: максимальная скорость
   
3. **Wattage Limits** (раздел с мощностью):
   - Watt Max: максимальная мощность

4. Нажмите "Write Motor Configuration" для сохранения

## ⚠️ Важно!

### Безопасность
- **НЕ устанавливайте** значения выше, чем поддерживает ваш VESC и мотор!
- Начинайте с **низких значений** и постепенно увеличивайте
- Проверяйте температуру VESC и мотора после изменений
- Используйте предохранитель на батарее!

### Типичные значения

**Электросамокат (36V, 500W)**:
- Motor Current Max: 30-40A
- Battery Current Max: 20-30A  
- Watt Max: 1000-1500W
- ERPM Max: 40000-50000

**Электровелосипед (48V, 1000W)**:
- Motor Current Max: 40-50A
- Battery Current Max: 25-35A
- Watt Max: 1500-2000W
- ERPM Max: 50000-60000

**Электроскейт (10S, 2x1500W)**:
- Motor Current Max: 60-80A (на мотор)
- Battery Current Max: 50-60A (на VESC)
- Watt Max: 2500-3000W (на мотор)
- ERPM Max: 70000-80000

## 🔧 Технические детали

### Обработка команд

1. **BLE → Дисплей → CAN → VESC**:
   - VESC Tool отправляет `COMM_GET_MCCONF`
   - Дисплей пересылает команду на CAN
   - VESC отвечает с полной конфигурацией
   - Ответ пересылается обратно в BLE

2. **Локальный парсинг** (упрощенный):
   - Модуль может извлечь основные лимиты из ответа
   - Кэширует значения для быстрого доступа
   - Проверяет актуальность данных (таймаут 10 секунд)

### Команды VESC

- `COMM_GET_MCCONF` (0x0E) - прочитать конфигурацию
- `COMM_SET_MCCONF` (0x0D) - сохранить конфигурацию (постоянно)
- `COMM_SET_MCCONF_TEMP` (0x30) - временно изменить (до перезагрузки)

## 📊 Примеры кода

### Пример 1: Чтение и вывод всех лимитов

```cpp
void print_vesc_limits() {
    if (!vesc_limits_is_valid()) {
        Serial.println("Лимиты не загружены");
        return;
    }
    
    const vesc_motor_limits_t* limits = vesc_limits_get();
    
    Serial.println("=== VESC Лимиты ===");
    Serial.printf("Мотор:   %.1fA / %.1fA (макс/торможение)\n",
                  limits->l_current_max, limits->l_current_min);
    Serial.printf("Батарея: %.1fA / %.1fA (макс/рекуперация)\n",
                  limits->l_in_current_max, limits->l_in_current_min);
    Serial.printf("Мощность: %.0fW / %.0fW (макс/торможение)\n",
                  limits->l_watt_max, limits->l_watt_min);
    Serial.printf("Скорость: %.0f ERPM\n", limits->l_erpm_max);
}
```

### Пример 2: Установка профиля "Эконом"

```cpp
void set_eco_profile() {
    // Сначала прочитаем текущие лимиты
    vesc_limits_request(0);
    delay(500);  // Подождем ответа
    
    if (!vesc_limits_is_valid()) {
        Serial.println("Не удалось прочитать лимиты");
        return;
    }
    
    // Уменьшим до 50% от максимальных
    const vesc_motor_limits_t* current = vesc_limits_get();
    vesc_motor_limits_t eco_limits = *current;
    
    eco_limits.l_current_max *= 0.5;
    eco_limits.l_in_current_max *= 0.5;
    eco_limits.l_watt_max *= 0.5;
    eco_limits.l_erpm_max *= 0.7;  // 70% скорости
    
    vesc_limits_set(0, &eco_limits);
    Serial.println("Профиль ЭКОНОМ активирован");
}
```

### Пример 3: Ограничение скорости для новичка

```cpp
void set_beginner_mode() {
    // Макс. скорость 20 км/ч (примерно 30000 ERPM для большинства моторов)
    vesc_limits_set_speed_max(0, 30000.0);
    
    // Макс. ток 30A (плавное ускорение)
    vesc_limits_set_current_max(0, 30.0, 25.0);
    
    Serial.println("Режим новичка активирован");
}
```

## 🐛 Отладка

### Включение логов

В файле `src/debug_log.h`:
```cpp
#define LOG_LIMITS_ENABLED  1  // Включить логи модуля
```

### Что вы увидите в Serial Monitor:

```
✅ [LIMITS] Limits module initialized
✅ [LIMITS] Requesting motor limits from VESC ID 0
✅ [LIMITS] Processing MCCONF response (512 bytes)
✅ [LIMITS] Limits updated:
✅ [LIMITS]    Motor Current Max:   60.00 A
✅ [LIMITS]    Motor Current Min:   -60.00 A
✅ [LIMITS]    Battery Current Max: 45.00 A
✅ [LIMITS]    Battery Current Min: -30.00 A
```

## 📝 TODO (будущие улучшения)

- [ ] Полный парсинг MCCONF (интеграция ConfigParams)
- [ ] UI для изменения лимитов на дисплее
- [ ] Профили: Эконом / Нормал / Спорт / Турбо
- [ ] Валидация значений перед отправкой
- [ ] Сохранение последних лимитов в NVS
- [ ] Автоматическое ограничение при низком заряде
- [ ] История изменений лимитов

## 💡 Советы

1. **Тестируйте безопасно**: Начинайте с низких значений, поднимите колесо/самокат
2. **Следите за температурой**: Высокие токи = высокая температура
3. **Используйте временные изменения**: `COMM_SET_MCCONF_TEMP` не сохраняет во flash
4. **Запишите оригинальные значения**: Перед экспериментами запомните заводские настройки
5. **Проверьте батарею**: Убедитесь, что BMS выдерживает установленные токи

## 🔗 Полезные ссылки

- [VESC Project](https://vesc-project.com/)
- [VESC Tool Documentation](https://vesc-project.com/vesc_tool)
- [Benjamin Vedder (автор VESC)](https://github.com/vedderb)

---

**Вопросы?** Смотрите подробную документацию в `VESC_LIMITS_USAGE.md`


