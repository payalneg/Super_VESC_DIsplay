# VESC Limits UI в экране Settings

## ✅ Что добавлено

В экран **Settings** добавлена новая секция "VESC Motor Limits" для управления ограничениями мотора.

### Новые UI элементы:

1. **Заголовок секции**: "=== VESC Motor Limits ===" (оранжевый цвет)

2. **Кнопка "Read Limits from VESC"** (синяя):
   - Читает текущие лимиты с VESC контроллера
   - Обновляет значения в spinbox'ах

3. **Motor Current Max (A)** - максимальный ток мотора:
   - Диапазон: 5.0 - 200.0 A
   - Шаг: 1.0 A
   - По умолчанию: 60.0 A
   - Кнопки +/- для регулировки

4. **Battery Current Max (A)** - максимальный ток батареи:
   - Диапазон: 5.0 - 200.0 A
   - Шаг: 1.0 A
   - По умолчанию: 45.0 A
   - Кнопки +/- для регулировки

5. **ERPM Max (x1000)** - максимальная скорость:
   - Диапазон: 10k - 200k ERPM
   - Шаг: 5k ERPM
   - По умолчанию: 60k ERPM
   - Кнопки +/- для регулировки

6. **Кнопка "Apply Limits to VESC"** (оранжевая):
   - Применяет новые лимиты к VESC контроллеру
   - Показывает статус применения

7. **Статусная метка**:
   - Показывает результат операции
   - Напоминание использовать VESC Tool для полной конфигурации

## 🎨 Визуальное оформление

- **Цвета кнопок**:
  - Минус (-): красный (#ff4444)
  - Плюс (+): голубой (#00a9ff)
  - Read: синий (#00a9ff)
  - Apply: оранжевый (#ff6600)

- **Цвета рамок spinbox**:
  - Motor Current: оранжевый (#ff6600)
  - Battery Current: янтарный (#ffaa00)
  - ERPM Max: зеленый (#00ff00)

- **Шрифты**:
  - Заголовки: Montserrat 16
  - Значения: Montserrat 24
  - Кнопки: Montserrat 16/24

## 🔧 Расположение

Секция добавлена между "Battery Calculation Mode" и кнопкой "Reset to Defaults".

Вертикальное положение адаптируется автоматически с помощью переменной `y_pos` и `spacing = 90`.

## 💻 Обработчики событий

### Добавлены следующие функции:

```c
// Основные кнопки
static void read_limits_btn_event_cb(lv_event_t *e);
static void apply_limits_btn_event_cb(lv_event_t *e);

// Motor Current кнопки +/-
static void motor_current_plus_btn_event_cb(lv_event_t *e);
static void motor_current_minus_btn_event_cb(lv_event_t *e);

// Battery Current кнопки +/-
static void battery_current_plus_btn_event_cb(lv_event_t *e);
static void battery_current_minus_btn_event_cb(lv_event_t *e);

// ERPM Max кнопки +/-
static void erpm_max_plus_btn_event_cb(lv_event_t *e);
static void erpm_max_minus_btn_event_cb(lv_event_t *e);
```

## 📝 Текущая функциональность

### Что работает сейчас:

- ✅ UI элементы создаются и отображаются
- ✅ Кнопки +/- изменяют значения
- ✅ Spinbox корректно показывают дробные числа
- ✅ Статусные сообщения обновляются

### Что нужно интегрировать:

- ⚠️ Вызов `vesc_limits_request()` при нажатии "Read Limits"
- ⚠️ Вызов `vesc_limits_set_current_max()` и `vesc_limits_set_speed_max()` при нажатии "Apply"
- ⚠️ Обновление значений spinbox'ов при получении данных от VESC

## 🔗 Интеграция с vesc_limits модулем

Когда модуль `vesc_limits` будет полностью интегрирован, нужно будет:

1. Добавить `#include "vesc_limits.h"` в `custom.c`
2. В `read_limits_btn_event_cb` добавить:
```c
uint8_t target_vesc_id = settings_wrapper_get_target_vesc_id();
vesc_limits_request(target_vesc_id);
```

3. В `apply_limits_btn_event_cb` добавить:
```c
uint8_t target_vesc_id = settings_wrapper_get_target_vesc_id();
float motor_current = lv_spinbox_get_value(settings_motor_current_spinbox) / 10.0f;
float battery_current = lv_spinbox_get_value(settings_battery_current_spinbox) / 10.0f;
float erpm_max = lv_spinbox_get_value(settings_erpm_max_spinbox) * 1000.0f;

vesc_limits_set_current_max(target_vesc_id, motor_current, battery_current);
vesc_limits_set_speed_max(target_vesc_id, erpm_max);
```

4. Создать функцию для обновления UI при получении данных:
```c
void update_limits_ui_from_vesc() {
    if (vesc_limits_is_valid()) {
        const vesc_motor_limits_t* limits = vesc_limits_get();
        
        lv_spinbox_set_value(settings_motor_current_spinbox, (int32_t)(limits->l_current_max * 10.0f));
        lv_spinbox_set_value(settings_battery_current_spinbox, (int32_t)(limits->l_in_current_max * 10.0f));
        lv_spinbox_set_value(settings_erpm_max_spinbox, (int32_t)(limits->l_erpm_max / 1000.0f));
        
        char buf[64];
        sprintf(buf, "Loaded: Mot %.1fA, Bat %.1fA, ERPM %.0fk", 
                limits->l_current_max, limits->l_in_current_max, limits->l_erpm_max/1000.0f);
        lv_label_set_text(settings_limits_status_label, buf);
    }
}
```

## ⚠️ Важно для пользователя

На данный момент **рекомендуется использовать VESC Tool через BLE** для полной конфигурации лимитов, так как:

1. VESC Tool имеет полную поддержку всех параметров MCCONF
2. BLE bridge пересылает все команды без изменений
3. Локальный UI - это упрощенный интерфейс для быстрой настройки основных параметров

## 🎯 Пример использования

1. Откройте экран Settings на дисплее
2. Прокрутите вниз до секции "=== VESC Motor Limits ==="
3. Нажмите "Read Limits from VESC" (появится статус)
4. Измените нужные значения кнопками +/-
5. Нажмите "Apply Limits to VESC"
6. Проверьте статусное сообщение

## 📂 Измененные файлы

- `Super_VESC_Display/custom/custom.c` - добавлены UI элементы и обработчики событий

---

**Примечание**: Полная интеграция с модулем `vesc_limits` будет выполнена на следующем этапе.

