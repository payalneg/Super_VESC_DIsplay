# Сохранение Trip и Amp-Hours в памяти

## Что реализовано

✅ **Автоматическое сохранение каждые 10 секунд:**
- Trip (пройденное расстояние) в метрах
- Amp-hours (потребленная емкость) в Ач

✅ **Восстановление при включении:**
- При включении устройства значения загружаются из NVS
- Продолжается отсчет с сохраненных значений
- Не теряется километраж и емкость при выключении контроллера

✅ **Функция сброса для замены аккумулятора:**
- `battery_calc_reset_trip_and_ah()` - сбрасывает trip и amp-hours в ноль
- Можно вызвать из UI при замене аккумулятора

## Как использовать

### Обычная работа (автоматически)

Ничего делать не нужно! Все работает автоматически:
1. При получении данных от VESC каждые ~100мс значения обновляются
2. Каждые 10 секунд текущие значения сохраняются в NVS
3. При выключении/включении контроллера значения восстанавливаются

### Сброс при замене аккумулятора

Добавьте в ваш код вызов функции сброса:

```cpp
#include "vesc_battery_calc.h"

// Где-то в обработчике кнопки или команды:
battery_calc_reset_trip_and_ah();
```

Это сбросит trip и amp-hours в ноль и очистит сохраненные данные.

## Новые/измененные файлы

### Новые файлы:
- `src/vesc_trip_persist.h` - заголовочный файл модуля
- `src/vesc_trip_persist.cpp` - реализация модуля
- `TRIP_PERSISTENCE_USAGE.md` - подробная документация (English)
- `TRIP_PERSISTENCE_README_RU.md` - краткая инструкция (русский)

### Измененные файлы:
- `src/vesc_rt_data.h` - добавлена функция `vesc_rt_data_get_amp_hours()`
- `src/vesc_rt_data.cpp` - интеграция модуля persistence
- `src/vesc_battery_calc.h` - добавлена `battery_calc_reset_trip_and_ah()`
- `src/vesc_battery_calc.cpp` - реализация сброса
- `src/ui_updater.cpp` - использование persistent amp-hours

## Как работает восстановление после выключения

### Пример:

```
1. Поездка: проехали 10 км, потребили 2.5 Ah
   → VESC показывает: tachometer_abs = 10000 м, amp_hours = 2.5
   → Сохранено в NVS: trip_total = 10000 м, ah_total = 2.5

2. Выключили контроллер
   → VESC: все сбросилось в 0
   → NVS: сохранено trip_total = 10000 м, ah_total = 2.5

3. Включили контроллер
   → VESC показывает: tachometer_abs = 0 м, amp_hours = 0
   → Загружено из NVS: trip_total = 10000 м, ah_total = 2.5
   → Рассчитан offset: trip_offset = 10000 м, ah_offset = 2.5
   → Отображается: 10.0 км, 2.5 Ah ✓

4. Продолжили поездку: проехали еще 5 км, потребили 1.2 Ah
   → VESC показывает: tachometer_abs = 5000 м, amp_hours = 1.2
   → Offset: trip_offset = 10000 м, ah_offset = 2.5
   → Отображается: (5000 + 10000) / 1000 = 15.0 км ✓
   → Отображается: 1.2 + 2.5 = 3.7 Ah ✓
```

## Настройка интервала сохранения

По умолчанию сохранение происходит каждые **10 секунд**.

Чтобы изменить интервал, отредактируйте файл `src/vesc_trip_persist.cpp`:

```cpp
// Найдите эту строку (примерно line 125):
if (now - last_save_time >= 10000) {  // 10000 = 10 секунд

// Измените на нужное значение в миллисекундах:
if (now - last_save_time >= 5000) {   // 5000 = 5 секунд
if (now - last_save_time >= 30000) {  // 30000 = 30 секунд
```

⚠️ **Примечание:** Более частое сохранение увеличивает износ flash-памяти.

## Логирование

Модуль выводит информацию в Serial Monitor:

При старте:
```
[SYSTEM] Trip persistence module initialized with saved state
[SYSTEM] Trip offsets calculated: Trip offset=10000.00 m, Ah offset=2.50
```

Каждые 10 секунд:
```
[SYSTEM] Trip persist: Trip=15.00 km (VESC=5000.00 m + offset=10000.00 m), Ah=3.70 (VESC=1.20 + offset=2.50)
```

При сбросе:
```
[SYSTEM] Resetting trip and amp-hours due to battery swap
[SYSTEM] Trip and amp-hours reset complete
```

## FAQ

**Q: Что будет при первом запуске?**  
A: Модуль начнет с нуля (trip = 0, amp-hours = 0) и начнет накапливать значения.

**Q: Что будет, если вручную сбросить счетчики в VESC?**  
A: Модуль определит уменьшение значений и автоматически скорректирует offset для сохранения непрерывности.

**Q: Можно ли сбросить только trip, но не amp-hours?**  
A: Сейчас функция `battery_calc_reset_trip_and_ah()` сбрасывает оба значения. Если нужен отдельный сброс, можно вызвать напрямую:
```cpp
#include "vesc_trip_persist.h"
trip_persist_reset();  // Сбросит и trip, и amp-hours
```

**Q: Влияет ли это на работу smart battery calculation?**  
A: Нет, это независимые модули. Smart battery calculation использует свои собственные сохраненные данные.

## Проверка работы

Для проверки работы модуля:

1. Запустите устройство
2. Проедьте небольшое расстояние (например, 100 м)
3. Подождите 10+ секунд (чтобы произошло сохранение)
4. Выключите контроллер VESC
5. Включите контроллер обратно
6. Проверьте, что trip показывает сохраненное значение (~0.1 км), а не ноль

В Serial Monitor должно появиться:
```
[SYSTEM] Trip state loaded: Trip=100.00 m, Ah=0.XX
[SYSTEM] Trip offsets calculated: Trip offset=100.00 m, Ah offset=0.XX
```

Готово! Теперь trip и amp-hours сохраняются в памяти! 🎉

